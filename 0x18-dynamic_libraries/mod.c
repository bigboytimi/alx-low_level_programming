#include <stdio.h>


/**
 * mod - get the remainder of two values
 * @a: first value
 * @b: second value
 * Return: mod of two values
 */

int mod(int a, int b){
	int result;
	result = a % b;
	return (result);
}
