#include <stdio.h>

/**
 * div - divide two values
 * @a: first value
 * @b: second value
 * Return: division of two values
 */

int div(int a, int b){
	int result;
	result = a / b;
	return (result);
}
