#include <stdio.h>


/**
 * mul - multiply two values
 * @a: first value
 * @b: second value
 * Return: Product of the two values
 */

int mul(int a, int b){
	int result;
	result = a * b;
	return (result);
}
