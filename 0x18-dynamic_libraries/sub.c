#include <stdio.h>

/**
 * sub - subtract values
 * @a: first value
 * @b: second value
 * Return : subtraction of two values
 */

int sub(int a, int b){
	int result;
	result = a - b;
	return (result);
}
