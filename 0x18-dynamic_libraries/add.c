#include <stdio.h>
#include <unistd.h>

/**
 * add - adds two values together
 * @a: first value
 * @b: second value
 * Return : Sum of two values
 */

int add(int a, int b){
	int sum;
	sum = a + b;
	return sum;
}
